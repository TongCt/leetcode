这道题有两种解法：
1 利用merge的思想，.py是用merge写的
先做两个list的合并
然后对k个list进行合并，合并的时候利用分治的思想进行合并复杂度位logK,总的复杂度为n*klog(k)
空间复杂度是递归栈的大小O(log(k))

2利用heap，.cpp文件
利用了C++的优先队列，python也有类似的库heapq
先把所有的list都push到队列中
然后得到最小的，把最小队列的最小值拿去，然后把余下的在放入优先队列中，在优先队列中维持堆得性质要log(k)的时间
在获取每一个元素中需要遍历一遍就是n*k
因此算法的复杂度是n*klog(k)
空间复杂度是堆的大小O(K)


